name: C/C++ CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v1
  
    - name: make
      run: cd chaos_calmer_15_05_1/
      run: ./scripts/feeds install -a
      run: ./make.sh df1a fullmask
